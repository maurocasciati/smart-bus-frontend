import React from "react";
import { View, Text } from "react-native";
import { styles } from "../styles/styles";

export default function DetalleRecorrido({ navigation }) {
	return (
		<View style={styles.container}>
			<Text>Pantalla detalle</Text>
		</View>
	);
}